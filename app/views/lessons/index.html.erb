<div class="container">
  <div class="row">
    <div class="col-lg-12">
      Display Lessons
    </div>
  </div>
  <% @lessons.each do |lesson| %>
  <%= lesson.name %>
  <%=link_to 'Edit Lesson', edit_lesson_path(lesson) %>
  <%end %>
</div>
